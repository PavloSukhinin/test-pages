import { Lecture, VideoWrapper, Level, Language, TimelineOfContents } from "../../../../components";
import { viktoriiaVasylenko2021Avatar } from "../../../../assets/images/avatars/intro-rounded";

<div>
  <h1>
    <strong>Как работать с Git</strong>
  </h1>
</div>

<Language
  currentLanguage="ru"
  languages={{
    en: "lectures/how-to-work-with-git/en",
    ua: "lectures/how-to-work-with-git/ua",
    ru: "lectures/how-to-work-with-git/ru",
  }}
/>

<div>
  <div class="author">
      <div class="avatar-wrapper">
        <object class="avatar-object" role="img" data={viktoriiaVasylenko2021Avatar} aria-label="Viktoriia Vasylenko"></object>
      </div>
      <div class="profile">
        <h3 class="name">Виктория Василенко</h3>
        <p class="about">
          Виктория работает в Binary Studio как Full stack developer после окончания Binary Studio Academy в 2020 году.        </p>
      </div>
    </div>
  <div class="bubble" style="margin: -0.5em 1em 1em;">
    <strong>Привет!</strong> 👋<br />Добро пожаловать на лекцию по Git. Здесь вы найдёте короткий конспект к лекции с основными Git командами и полезными ссылками. Погнали!
  </div>
</div>

<div id="table-of-contents" class="table-of-contents" style="margin-top: 2em;">
  <h2><strong>План лекции</strong></h2>
  <TimelineOfContents timeline={[{
    linkTo: "#setting-up-the-repository",
    title: "Настройка репозитория"
  }, {
    linkTo: "#practicing-git",
    title: <span>Основные <code>git</code> команды</span>
  }, {
    linkTo: "#git-flow",
    title: "Git flow"
  },  {
    linkTo: "#undo-changes",
    title: "Отменить изменения"
  }]} />
</div>

<VideoWrapper duration="38m 2s">
  <iframe src="https://www.youtube.com/embed/HJ7NW6zatXo" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</VideoWrapper>

<Level id="setting-up-the-repository" number="1" name="Настройка репозитория" difficulty="Beginner" objectives="How to create and clone the repo" />

1. Перед тем как склонить репозиторий вам нужно будет настроить токен – PAT (personal access token) или SSH ключи.
   Это может пригодиться -
   <a href="https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token"><strong>How to set up PAT</strong></a>, 
   <a href="https://docs.github.com/en/authentication/connecting-to-github-with-ssh"><strong>How to set up SSH keys</strong></a>.  

2. Чтобы склонить репозиторий воспользуйся `git clone` командой + URL на репозиторий.  

   При https это будет выглядеть так:  
   ```
   git clone https://github.com/<my-username>/homepage.git
   ``` 
   При SSH: 
   ```
   git clone git@github.com:<my-username>/homepage.git
   ``` 

3. Используй `git remote show origin` чтобы просмотреть ссылку на удаленный репозиторий.

4. `git status` команда отобразит состояние вашего репозитория и staging area.
    Используй данную команду, чтобы проверить, какие файлы добавлены в staging area и будут включены в комит.


<Level id="practicing-git" number="2" name={<span>Основные <code>git</code> команды</span>} difficulty="Beginner" objectives="How to create commits and branches"  />

1. Чтобы добавить файлы в staging area используй `git add` команду + название файла.
   Можно использовать `git add .` команду, чтобы добавить <strong>все</strong> измененные и новые файлы.  

2. Чтобы создать комит используй `git commit` команду. А чтобы создать комит описанием используй
   ```
   git commit -m "Your message here"
   ```  

3. Используй `git branch` команду, чтобы просмотреть локальные ветки и `git checkout branch-name`, чтобы переключиться на ветку.
   Чтобы создать новую ветку и переключиться на нее можешь воспользоваться `git checkout -b branch-name` командой. 

4. `git log` показывает историю комитов на ветке.  

5. Чтобы обновить удаленную ветвь, используй `git push` команду.
    Если твоей локальной ветви не существует на удаленном репозитории тебе нужно будет ввести `git push --set-upstream branch-name`
    или сокращенно `git push -u branch-name`, что означает то же. Или ты можешь явно указать, в какую remote ветку ты хочешь
    запушить свои изменения – вот так `git push origin branch-name`.  

6. Git stash удобен, если вы еще не готовы закоммитить свои изменения. `git stash` 'застешит' изменения, `git stash list` покажет все доступные стеши,
    а `git stash pop` применит последний стеш – вернет изменения назад.

7. Чтобы просмотреть изменения коллег, тебе понадобится `git fetch` команда. `git fetch` загружает
    комиты, файлы, новые ветки из удаленного репозитория в локальный. После этого вы можете захотеть
    смерджить изменения удаленной ветви в локальную (для этого есть команда `git merge branch-name`).
    Или вы можете воспользоваться командой `git pull`, чтобы скачать изменения из remote репозитория и
    смерджить в локальную ветвь за один раз.

8. В случае если вы забыли добавить файлы к последнему комиту или совершили ошибку в описании вы можете воспользоваться командой `git commit --ammend`.
 
<Level id="git-flow" number="3" name="Git flow" difficulty="Intermediate" objectives="How to create pull requests" />

Существует рекомендуемый сценарий при работе с репозиторием:

1. Create branches – создавать отдельную ветвь на каждую таску/feature

2. Add commits – делать небольшие комиты с понятными описаниями

3. Open a Pull requests - создавать pull requests для обсуждения, просмотра и утверждения ваших изменений

4. Merge to the main branch – мерджить изменения в главную ветвь


<Level id="undo-changes" number="4" name="Отменить изменения" difficulty="Hard" objectives="How to revert or reset commits" />

Есть две команды, чтобы отменить изменения: `git revert` и `git reset`.

1. `git revert HEAD` создает новый комит с обратными изменениями к последнему комиту.

2. `git reset <commit>` оочищает историю комитов вплотную к заданному.


export const metadata = {
  orderId: 1,
  author: "Viktoriia Vasylenko",
  title: "How to work with Git",
  description: "Виктория рассказывает об основах <strong>Git</strong> и распространенных командах. А также показывает весь процесс работы с Git: от клонирования репозитория до создания пул-реквестов.",
  keywords: ["hola", "guapa"],
  duration: "38 min",
  publishedAt: "2021-12-08T10:00:00",
  hiddenFromMainPage: true,
}

export default Lecture;
